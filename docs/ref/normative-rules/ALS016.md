# ALS016
この業界のベスト・プラクティス・ルールでは、幾何学的計算を行い、各車両間の幾何学的連続性を評価する。  
連続 `IfcCurveSegment`アライメント表現を構成する連続したエンティティ。

このルールは、形状表現（ジオメトリ）のみをチェックし、次のようなことは行わない。  
アライメントのセマンティック（ビジネスロジック）定義にある情報を利用する。

の`Transition` 。   
[`IfcCurveSegment`](https://standards.buildingsmart.org/IFC/RELEASE/IFC4_3/HTML/lexical/IfcCurveSegment.htm)  
それらは、モデル作者が意図した  
セグメント間の連続性。

値が`.CONTINUOUS.` の場合、ALS016 の警告は位置の不連続性に対してのみ出される。  
(接線ではない)。  
同様に、`.DISCONTINUOUS.` という値は、ALS016に対する警告が発せられないことを意味する、  
フルストップ