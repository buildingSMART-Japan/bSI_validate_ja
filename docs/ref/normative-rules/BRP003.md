# BRP003
このルールは、平面であると示された幾何学的な面が実際に同じ平面上にあることを確認するものである。


## アプローチ
1. 外側の境界から平面を導出する。法線にはNewellの方法を使い、入力点の平均からdを計算する。

1. その平面に点を投影し、各投影距離が表現コンテキストの精度内であることをチェックすることによって、外側と内側の境界を検証する。

```{note}
All calculations are performed in 128-bit floating point.
```
